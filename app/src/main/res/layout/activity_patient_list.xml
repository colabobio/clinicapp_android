<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
       >

    <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/txtPatientListHeader"
            style="@style/txtHeaderTitle"
            android:layout_alignParentTop="true"
            android:text="@string/patient_list_title" />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvPatient"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/txtPatientListHeader"
            android:background="@color/colorAppBackground"
            android:padding="@dimen/dimen_10"
            app:layoutManager="LinearLayoutManager"
            tools:context=".ui.studydata.patient.SearchPatientActivity"
            tools:listitem="@layout/row_patient" />


    <TextView
            android:id="@+id/emptyView"  style="@style/txtLabelHeader_noMargin"
            android:layout_width="wrap_content" android:layout_centerInParent="true"
            android:layout_margin="@dimen/dimen_15"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:text="@string/empty_patient_warning"
            android:visibility="gone"

    />

</RelativeLayout>
